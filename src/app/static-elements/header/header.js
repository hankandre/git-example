import angular from 'angular';
import HeaderComponent from './header.component';


const HeaderModule = angular.module('headernav', [

    ])
    .component('headernav', HeaderComponent);

export default HeaderModule;